services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=4321
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
    volumes:
      - db-data:/app/data
    expose:
      - "80:4321"

volumes:
  db-data: